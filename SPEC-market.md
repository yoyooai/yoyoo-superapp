# 超级应用市场 —— 设计（2026-09-02，等苏白拍板后施工）

**由来**：苏白 09-02 提议「每个人都可以发布自己的超级应用，也可以安装别人的；安装后出现在侧栏」，
并在我给出"不寄生宿主市场"的替代方案后回「**我觉得可以设计一下了**」。
本文是那个替代方案的施工级设计。**写完先给他看，不动代码。**

---

## 0. 先说清边界：为什么不放进宿主自己的「市场」

三条，都是查过源码的，不是偏好问题：

1. **它的市场类型是数据库层面写死的四种**（`expert` / `expert_team` / `skill` / `connector`，
   MySQL ENUM），后端校验 + 前端页签也按这四种写死。加"应用"这第五种＝改它的表结构、模型、
   校验、前端 ⇒ 撞我们那条尺子：**要动它骨头的就不做**（不然它每次升级我们都要手工合一遍）。
2. **它的"安装"和我们的"安装"不是一回事**。它的安装是把插件装到某个 workspace / runtime
   （给它那套 Loop 运行时用）；我们的安装是**把一个应用蓝图复制到某个人名下、变成他能打开的东西**。
   硬塞会让两套语义互相污染。
3. **应用生态是护城河，不该长在别人家的表里**。一旦长上去，"整个拔下来换个壳"那条铁线就废了。

⇒ **同样的体验，我们自己长**：发布 / 市场页签 / 安装到你名下 / 钉到侧栏，全在
`/superapp` 这一个入口内部，宿主只提供"你是谁"。

---

## 1. 用户看到什么（这一节是验收口径）

「应用」入口内部两个页签：**我的应用** ｜ **市场**

| 动作 | 用户视角 | 落到系统里是什么 |
|---|---|---|
| 发布 | 我的应用 → 某个应用 → 「发布到市场」→ 填一句简介 → 发布 | 给当前蓝图**照一张快照**，建一条 listing（v1） |
| 逛 | 市场页签：卡片列表（图标 / 名字 / 一句简介 / 作者 / 安装数） | 只读列表接口，不下发蓝图 |
| 装 | 点「安装」→ 立刻出现在"我的应用"里，可以打开、可以改 | **把快照复制一份到我名下**，记来源 |
| 钉 | 应用详情 → 「钉到侧栏」 | 侧栏多一个图标，直接打开这个应用 |
| 更新 | 作者发新版 → 装过的人看到"有更新"，自己点了才更新 | 新建 v2 快照；安装者的副本不动，除非他点 |

🔴 **侧栏默认仍然只有一个「应用」入口**。钉出来的是用户自己选的，且有上限（见 §4.2）——
不然装十个侧栏就爆了，那是把"生态"做成"垃圾场"。

---

## 2. 三个关键设计决定（以及为什么）

### 2.1 发布 = 快照，不是活引用

listing 存**独立的一份蓝图快照 + 版本号**，不是指向作者那条 `apps` 记录。

*为什么*：作者发布完还会继续改自己那个应用。如果是活引用，他一手把自己的应用改坏，
**所有装过的人当场一起坏**。快照让"我的应用"和"我发布的东西"是两件事 —— 这也符合直觉：
你发布的是那一版，不是你以后所有的版本。

### 2.2 安装 = 复制到你名下（copy-on-install）

安装动作 = 在 `apps` 里给安装者插一条新记录，蓝图取自 listing 的当前版本快照，
并记下 `source_listing_id` / `source_version`。

*为什么*：这是苏白原话"**安装到自己名下**"的字面实现，也让安装后的自由修改成为默认权利
（装完就是你的，改坏了也只坏你自己那份）。代价是作者更新后不会自动流到你那儿 —— 这正是下一条。

### 2.3 更新是"通知"，不是"推送"

装过的人在列表里看到"有新版"，点了才更新；更新 = 用新快照覆盖他那条 `apps` 的蓝图
（**先自动备份旧蓝图一份**，允许一键回退）。

*为什么*：自动推送等于作者对所有安装者的机器有写权限。宁可多一次点击。

---

## 3. 数据模型（全部在我们自己的 SQLite，不动宿主任何表）

现有 `apps` 表加两列（幂等 `ALTER TABLE ... ADD COLUMN`，老数据为 NULL 即"自己造的"）：

```sql
source_listing_id TEXT,     -- 从市场装来的：来源 listing
source_version    INTEGER   -- 装的是哪一版（用来算"有更新"）
```

三张新表：

```sql
CREATE TABLE listings (          -- 一条"上架的东西"
  id          TEXT PRIMARY KEY,
  space_id    TEXT NOT NULL,     -- 预留：本期只有一个 Space，写死填 OCTO_SPACE_ID
  author_uid  TEXT NOT NULL,
  app_id      TEXT NOT NULL,     -- 作者那边的原始应用（只作溯源，不作数据来源）
  name        TEXT NOT NULL,
  icon        TEXT,
  summary     TEXT NOT NULL,     -- 一句话简介，<= 140 字
  version     INTEGER NOT NULL,  -- 当前版本号，从 1 开始
  status      TEXT NOT NULL,     -- listed | delisted   (预留 pending_review)
  installs    INTEGER NOT NULL DEFAULT 0,
  created_at  INTEGER NOT NULL,
  updated_at  INTEGER NOT NULL
);
CREATE TABLE listing_versions (  -- 每一版的蓝图快照（不可变）
  listing_id  TEXT NOT NULL,
  version     INTEGER NOT NULL,
  blueprint   TEXT NOT NULL,
  note        TEXT,              -- 这版改了什么（作者自己写）
  created_at  INTEGER NOT NULL,
  PRIMARY KEY (listing_id, version)
);
CREATE TABLE installs (          -- 谁装了什么（去重 + 安装数 + "有更新"）
  listing_id  TEXT NOT NULL,
  uid         TEXT NOT NULL,
  app_id      TEXT NOT NULL,     -- 装出来的那条 apps.id
  version     INTEGER NOT NULL,
  created_at  INTEGER NOT NULL,
  PRIMARY KEY (listing_id, uid)
);
CREATE TABLE pins (              -- 钉在侧栏的应用（每人一份，有序）
  uid         TEXT NOT NULL,
  app_id      TEXT NOT NULL,
  sort        INTEGER NOT NULL,
  PRIMARY KEY (uid, app_id)
);
```

🔴 **`space_id` 现在没用但必须现在加**：本期是单 Space 部署，市场对所有登录用户可见。
哪天多 Space，过滤条件加在查询里就行，**不用迁表**。加一列的成本是零，改表的成本不是。

---

## 4. 接口与前端

### 4.1 接口（全部挂在现有 `/yoyoo/v1` 下，沿用现有身份逻辑）

| 方法 | 路径 | 说明 |
|---|---|---|
| GET | `/market/listings?q=&page=` | 列表（只读；不含蓝图） |
| GET | `/market/listings/:id` | 详情（含版本历史摘要；**仍不含蓝图**） |
| POST | `/market/listings` | 发布：`{app_id, summary}` → 照快照建 v1 |
| POST | `/market/listings/:id/versions` | 发新版：`{note}` → 从作者当前应用再照一张 |
| DELETE | `/market/listings/:id` | 下架（作者自己；`status=delisted`，**不删数据**） |
| POST | `/market/listings/:id/install` | 安装 → 复制到我名下，返回新 `app_id` |
| POST | `/apps/:id/sync` | 更新到来源的最新版（旧蓝图自动备份） |
| GET/PUT | `/pins` | 读 / 写我的侧栏钉位（整表覆盖，带上限校验） |

🔴 **蓝图只在两个时刻离开服务端**：安装时（复制给你）和你打开自己应用时。
逛市场**不下发蓝图** —— 不然"浏览"就等于"拿走全部实现"。

### 4.2 钉到侧栏（可行性已取证，不是设想）

宿主 `MenusManager.register(sid, factory, sort)` 的**工厂可以返回 `undefined` 表示不展示**，
并且有 `refresh()` 触发 NavRail 重渲染 —— 宿主自己就是这么按 remoteConfig 做运行时显隐的
（取证：`packages/dmworkbase/src/Service/Menus.ts:16-20`，注释原文
"工厂可返回 undefined 表示「当前不展示该菜单」…配合 refresh() 可实现按 remoteConfig 运行时显隐"）。

⇒ 做法：mount 时注册 **6 个钉位**（`yoyoo-pin-1..6`，sort 排在「应用」之后），
每个工厂读内存里的 pins 快照：钉了就返回 Menus（图标 = 应用图标，点击直达
`/superapp?app=<id>`），没钉就返回 `undefined`。改完调 `refreshEntries()`。

**上限 6 个**，写死。理由：这是别人家的侧栏，我们借了一格已经算客气；把它填满是失礼，
也是给用户造垃圾场。用户想多放 —— 那说明该做"我的应用"里的置顶区，不是继续占侧栏。

**铁线保持不变**：以上全部经 `HostAdapter` 走 —— 给 `HostEntry` 加可选 `visible?: () => boolean`，
给 `HostAdapter` 加可选 `refreshEntries?(): void`，`@octo/*` 依旧只出现在 `src/host/`。
换壳时新宿主不支持动态入口，只是钉功能降级为"置顶区"，其它一律照跑。

---

## 5. 安全与边界（每条都要有否定用例）

1. **入库只有一道门**：listing 的蓝图也必须过 `normalizeForStore()` —— 和 `apps` 共用同一份
   收敛器。现在结构测试已经守着"sanitize 只有一份"，新增路径必须一起守。
2. **发布前扫一遍私货**：蓝图里可能有作者自己填的内容（token、手机号、内部链接）。发布时做
   一次启发式扫描，**命中就拦下并告诉他命中了什么**，让他自己决定（不静默发布，也不静默删改）。
   > 这是本设计里我最担心的一件：发布是"从私有变公开"的**不可逆**动作。
3. **只有作者能发新版 / 下架**：`author_uid` 必须等于当前登录 uid，否则 403。
4. **安装只能装 `status=listed`**：下架的、不存在的 → 404（不泄露"存在但下架"）。
5. **不能装自己没资格看的**：本期单 Space 全员可见；`space_id` 不匹配 → 404。
6. **下架不删数据**：已装的人不受影响（他那份是副本），listing 只是不再出现在列表里。
7. **安装数是统计不是权威**：从 `installs` 表实时 count，不维护可漂移的计数器。
8. **速率**：发布 / 发新版每人每分钟 ≤ 3 次；安装无限制（幂等：重复安装返回已有那条）。

---

## 6. 会报红的检查（150 分线第三条）

- **结构测试**：新增 listing/install 两条写入路径都过 `normalizeForStore`（和现有三条同一个断言）
- **否定用例（冒烟）**：未登录 401 ／ 非作者发新版 403 ／ 装下架的 404 ／ 装不存在的 404 ／
  跨 Space 404 ／ 钉超过 6 个 400 ／ 重复安装返回同一条（不产生两份）
- **快照不可变**：作者发布后改自己的应用，listing 的 v1 蓝图**逐字节不变**（这条防的是 §2.1 那个坑）
- **敏感扫描器单测**：给几条含 `sk-`、手机号、内网地址的蓝图，必须全部命中
- **前端**：钉位工厂在 pins 为空时返回 `undefined`（不留 6 个空图标在侧栏）
- **反向验证**：把 `visible` 判断去掉 → 侧栏出现空图标 → 测试必须红

---

## 7. 施工顺序（粗估 1~2 天，与我先前答他的口径一致）

| 步 | 内容 | 产出 |
|---|---|---|
| 1 | 三张表 + `apps` 两列 + 收敛器接上新路径 | 结构测试绿 |
| 2 | 发布 / 列表 / 详情 / 下架 | 冒烟含否定用例 |
| 3 | 安装 / 更新 / 来源追溯 | 快照不可变那条测试 |
| 4 | 市场页签 UI（列表 + 详情 + 安装态） | 本机 e2e |
| 5 | 钉侧栏（`visible` + `refreshEntries`） | 反向验证报红 |
| 6 | 发布前敏感扫描 + 文案 | 扫描器单测 |
| 7 | 上线（照 `ship.sh`，不手改） | 线上只读冒烟 |

---

## 8. 明确不做（写下来防止以后偷偷长出来）

- **不做跨 Space / 公网市场**：这是企业 IM，"公开"的边界必须由部署方定，不由我们默认。
- **不做人工审核流程**：本期作者自负 + 举报下架足够。`status` 里预留 `pending_review`，
  开关留着，但**不做没人用的审核后台**。
- **不做付费 / 打赏**：一碰钱就要谈发票、退款、税 —— 那是另一个产品。
- **不自动推送更新**（§2.3）。
- **不把应用塞进宿主的市场表**（§0）。
- **不为了钉侧栏改宿主一行代码**（§4.2 用的是它自己公开的用法）。

---

## 9. 留给苏白拍的三件

1. **侧栏钉位上限 6 个**，同意吗？（我倾向 6；他要 3 或 10 都行，改一个常数）
2. **发布要不要审核**：我建议本期不审（作者自负 + 下架），他要审我就把 `pending_review`
   接起来，多一个"我发布的"待审列表。
3. **市场里能不能装 AI 造的应用**：现在 `apps` 有 `created_by` 字段（human / ai）。
   我倾向**照原样展示、不做区分门槛** —— AI 造的也是他的作品；但要在卡片上如实标注来源。
